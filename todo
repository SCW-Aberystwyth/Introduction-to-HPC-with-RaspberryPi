most minimal sbatch job
sftp short guide

reference guide
pasting

sftp/scp available via cmd on IS windows machines
also via the git bash shell

diagram of login node to clusters

file system images need updating

import os on python example